import { SidebarInset, SidebarProvider } from "@/components/ui/sidebar";
import "./globals.css";
import { ClerkProvider } from "@clerk/nextjs";
import { AppSidebar } from "@/components/app-sidebar";
import { SiteHeader } from "@/components/site-header";

export const metadata = {
  title: "Fashi E-commerce",
  description: "Generated by create next app",
};

export default function RootLayout({ children, basket }) {
  return (
    <ClerkProvider>
      <html lang="en">
        <body suppressHydrationWarning>
          {/* 1. min-h-screen ensures the layout takes up the full height */}
          <SidebarProvider className="flex flex-col min-h-screen">
            <SiteHeader />

            {/* 2. Added 'flex flex-1' here to align Sidebar and Content horizontally */}
            <div className="flex flex-1 overflow-hidden">
              <AppSidebar />

              <SidebarInset className="flex-1 overflow-y-auto">
                {/* 3. Added a container with controlled padding */}
                <main className="p-4 md:p-6">
                  {children}
                  {basket}
                </main>
              </SidebarInset>
            </div>
          </SidebarProvider>
        </body>
      </html>
    </ClerkProvider>
  );
}
