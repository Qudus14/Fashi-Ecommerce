import {blockContentType} from './blockContentType'
import {categoryType} from './categoryType';
import {orderType} from './orderType';

export const schema = {
  types: [blockContentType, categoryType, orderType],
}
